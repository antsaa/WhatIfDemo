{
    "env": {
        "name": "#{envName}"
    },
    "appInsights": {
        "instrumentationKey": "#{appInsightsKey}"
    },
    "logging": {
        "console": true,
        "appInsights": true
    },
    "facebook": {
        "appId": "#{facebookAppId}"
    },
    "api": {
        "url": "https://#{apiUrl}.azurewebsites.net"
    }
}